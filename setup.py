from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='END to END ML Operation with DVC, DAGSHUB, MLFLOW',
    author='AbdeALI',
    license='MIT',
)
